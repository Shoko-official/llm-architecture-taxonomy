# Tests

This directory is reserved for future validation tests.

Current validation is handled by `scripts/validate_repo.py`.

## Milestone 3 Validation

We have added automated validation rules in `scripts/validate_repo.py` to verify:
- Sub-layer components in the Agent Layer (`Planner`, `Tool Executors`, `Trace`).
- Sub-layer components in the Memory Layer (`Short-term Cache`, `Episodic Cache`, `Long-term Cache`).

